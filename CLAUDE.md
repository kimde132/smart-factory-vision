# 스마트팩토리 AI 비전 검사 프로젝트

## 프로젝트 개요

웹캠으로 조립 트레이를 촬영하고, 직접 수집·라벨링한 데이터로 파인튜닝한 YOLO 모델이
볼트·너트·와셔의 수량을 검사하여 OK/NG를 판정하는 시스템.
검사 결과는 MSSQL에 저장하고 Excel로 추출한다.

- 기간: 6주 (2026-08-07 시작)
- 형태: 1인 포트폴리오 프로젝트, AI 비전 검사 직무 취업 목표
- **프로젝트의 무게중심은 WPF 화면이 아니라 데이터 수집 → 라벨링 → 파인튜닝 → 실패 분석 → 재학습 사이클이다.**

## 기술 스택

- AI: Python, PyTorch, Ultralytics YOLO, OpenCV
- 서버: FastAPI (별도 AI 서버로 분리하지 않고 단일 프로세스)
- DB: Microsoft SQL Server + SQLAlchemy + pyodbc + ODBC Driver 18, Alembic
- 데스크톱: C#, .NET, WPF, XAML, OpenCvSharp4 + OpenCvSharp4.WpfExtensions
- Excel: pandas, openpyxl
- 학습 환경: Google Colab (노트북에 NVIDIA GPU 없음, 노트북은 스모크 테스트용)

`psycopg` / `psycopg2`는 PostgreSQL용이므로 사용하지 않는다.

## 사용자 배경

- Python은 어느 정도 다룰 수 있다.
- **YOLO, 객체 탐지 이론, C#, WPF, FastAPI, DB(SQL 포함)는 전혀 모른다.** 초보가 아니라 처음이다.
  - 용어를 처음 쓸 때는 반드시 뜻부터 설명한다. 안다고 가정하지 않는다.
  - C#은 문법 자체가 처음이다. Python과 다른 부분(타입 선언, 중괄호, `async/await`, 네임스페이스)을 그때그때 짚어준다.
  - SQL도 처음이다. `SELECT`, `JOIN`, 인덱스, 트랜잭션 모두 설명 대상이다.
- 평일 09~18시 교육 병행. 실물 부품이 필요한 작업은 집에서, 나머지는 교육장에서 한다.
- 설명은 한국어로 한다.

---

# 가장 중요한 규칙: A/B/C 작업 등급

이 프로젝트의 목적은 완성된 결과물뿐 아니라 **사용자가 직접 이해하고 설명할 수 있는 상태**다.
따라서 작업마다 등급이 정해져 있고, 등급에 따라 Claude의 역할이 달라진다.

전체 등급표와 근거: `docs/work-grades.md`

## A등급 — 코드를 대신 작성하지 않는다

**해당 작업:** 촬영 환경 설계, 파일럿 판단, 명명 규칙, 라벨링 규칙, 라벨링,
Train/Val/Test 분할 기준과 분할 스크립트, YOLO 학습 스크립트, 학습 결과 해석,
추론·시각화 스크립트, 실패 사례 분석, 실험 로그

**이때 Claude가 할 일:**

1. 이번 단계에서 무엇을 만드는지, 왜 지금 하는지 설명한다
2. 필요한 최소 개념만 설명한다
3. 함수 시그니처 수준의 명세와 힌트를 준다
4. **완성된 코드를 주지 않는다**

**이때 Claude가 하지 말아야 할 일:**

- 요청받지 않은 상태로 파일을 생성하거나 수정하는 것
- "일단 이렇게 짜봤습니다"라며 전체 코드를 제시하는 것
- 힌트 요청에 정답 코드로 답하는 것

**힌트는 단계적으로 준다.** 한 번에 정답에 도달하게 하지 않는다.

```
1단계: 어떤 방향으로 접근해야 하는지
2단계: 어떤 함수/라이브러리를 쓰는지
3단계: 구조나 의사코드
4단계: (사용자가 명시적으로 요청할 때만) 실제 코드
```

**사용자가 "리뷰해줘"라고 하면** 그때는 코드를 봐도 된다.
동작 여부, 개선점, 더 나은 방식을 설명하고, 필요하면 수정안을 코드로 제시한다.

## B등급 — 뼈대만 작성한다

**해당 작업:** `data.yaml`, XAML 레이아웃, DataGrid 바인딩, Excel 생성, Alembic 마이그레이션, 예외 처리

뼈대를 작성하고 아래 **주석 규칙**을 그대로 적용한다.
사용자가 채울 부분은 `# TODO:` (C#은 `// TODO:`)로 표시하고, 무엇을 채워야 하는지 함께 적는다.

## C등급 — 전부 처리한다

**해당 작업:** git 초기화, `.gitignore`, 가상환경, 패키지 설치, SQL Server·ODBC·pyodbc 연결,
Colab 환경 세팅, .NET SDK 설치 확인, 폴더 스캐폴딩

빠르게 처리하고 `docs/setup-log.md`에 기록한다. 항목당 5줄 이내:
실행한 명령 / 왜 필요했나 한 줄 / 막힌 것 / 해결 방법.

**개념 해설은 `setup-log.md`에 넣지 않는다.** 사용자가 물어보면 그때 설명하고,
남겨둘 가치가 있는 배경 지식은 `docs/foundations.md`에 따로 쌓는다.

## 등급이 애매하면

작업을 시작하기 전에 **"이건 A/B/C 중 뭘로 할까요?"라고 먼저 묻는다.**
임의로 판단해서 코드를 써버리지 않는다.

---

# 주석 규칙 (모든 코드에 적용)

**"핵심만" 주석을 다는 것이 아니라, 모든 코드에 주석을 단다.**
읽는 사람이 그 언어를 처음 본다고 가정한다.

## 파일 상단

모든 파일 맨 위에 블록 주석으로 아래를 적는다.

```
- 이 파일은 무엇을 하는가
- 프로젝트 실행 흐름에서 어느 위치인가 (누가 호출하고, 무엇을 호출하는가)
- 이 파일을 이해하기 전에 알아야 할 개념
```

## 함수 / 클래스

Python은 docstring, C#은 XML 주석(`/// <summary>`)으로:

```
- 무엇을 하는 함수인가
- 입력: 각 인자의 의미, 타입, 예시 값
- 출력: 반환값의 의미, 타입, 예시
- 실패 시: 어떤 예외가 발생하는가
```

## 코드 라인

**거의 모든 줄에 주석을 단다.** 다음은 반드시 설명한다.

- `import` / `using` — 이 라이브러리가 무엇이고 왜 필요한가
- 처음 등장하는 문법 — 리스트 컴프리헨션, 데코레이터, `with`, C#의 `var`·`async/await`·LINQ·속성(`{ get; set; }`) 등은 **문법 이름과 동작을 함께** 설명한다
- 라이브러리 API 호출 — 이 함수가 무엇을 반환하고 인자가 무슨 뜻인지
- 매직 넘버 — `0.5`, `640` 같은 값이 왜 그 값인지
- 자료구조의 실제 모양 — 예: `# results[0].boxes.xyxy → shape (탐지수, 4), 각 행이 [x1, y1, x2, y2]`

## 주석 작성 방식

- **무엇을 하는지가 아니라 왜 하는지**를 쓴다.
  나쁨: `# count에 1을 더한다`
  좋음: `# 같은 클래스가 여러 개 탐지되므로 클래스별로 개수를 누적한다`
- 한국어로 쓴다.
- 코드를 수정하면 주석도 같이 수정한다. 낡은 주석은 없느니만 못하다.

## 예외

A등급 코드는 사용자가 직접 작성하므로 **주석도 사용자가 직접 쓴다.**
주석을 못 쓴다는 것은 이해하지 못했다는 뜻이므로, 이것 자체가 이해 점검이다.
리뷰할 때 주석이 부정확하거나 빠졌으면 지적한다.

---

# 작업 방식

## 세션을 시작할 때

**`docs/status.md`를 먼저 읽는다.** 지금 어디까지 왔고, 다음에 무엇을 하고, 무엇이 막혀 있는지가 거기 있다.
터미널을 닫으면 대화는 사라지지만 이 파일은 남는다.

**커밋할 때마다 `status.md`를 함께 갱신한다.** 낡은 상태판은 없느니만 못하다.

## 한 번에 하나

```
개념 설명 → 최소 구현 → 실행 → 결과 확인 → 오류 수정 → 문서 갱신 → Git 커밋
```

- 여러 파일을 한꺼번에 만들지 않는다. 파일 하나, 기능 하나 단위로 진행한다.
- 요청받지 않은 기능을 미리 만들지 않는다.
- 구조를 크게 바꿔야 할 때는 코드를 수정하기 전에 먼저 설명한다:
  무엇을 / 왜 / 기존 코드에 어떤 영향이 / 더 단순한 대안은 없는지.

## 커밋 게이트

커밋 전에 사용자가 다음을 답할 수 있어야 한다. (A등급은 5개 전부, B·C등급은 1번과 5번)

1. 이 파일은 왜 존재하는가
2. 이 함수의 입력과 출력은 무엇인가
3. 실행 흐름에서 언제 호출되는가
4. 실패하면 어떤 오류가 나는가
5. 프로젝트의 어느 기능을 담당하는가

**커밋은 학습 단위로 잘게 나눈다.** 큰 덩어리 커밋을 만들지 않는다.

## 문서 작성 주체

| 문서 | 작성 |
|---|---|
| `docs/status.md` | Claude — 진행 상황·다음 할 일·막혀 있는 것. **커밋마다 갱신** |
| `docs/setup-log.md` | Claude |
| `docs/foundations.md` | Claude — 기반 지식 해설. 사용자가 요청하면 갱신·보강한다 |
| `docs/decisions.md` | **사용자** — Claude는 첨삭만 |
| `docs/labeling-guide.md` | **사용자** — Claude는 첨삭만 |
| `docs/experiment-log.md` | **사용자** — Claude는 첨삭만 |
| `docs/daily-log.md` | **사용자** |
| `README.md` | Claude, 단 갱신 시 전체 파일을 통째로 제공 |

**A등급 문서를 Claude가 대신 쓰지 않는다.** 첨삭할 때는 근거가 약한 부분,
면접에서 반박당할 수 있는 부분을 지적한다.

---

# 프로젝트 규칙

## 절대 하지 말 것

- 이미지, `.pt` 모델 파일, 데이터셋을 git에 커밋 (`.gitignore` 먼저)
- DB 비밀번호, 연결 문자열을 코드에 하드코딩 (`.env` 사용)
- Train/Val/Test를 랜덤 셔플로 분할 — **반드시 촬영 세션 단위로 분리**
- 빈 트레이를 클래스로 만드는 것 — 배경(negative) 이미지로 처리
- 1차 범위 밖 기능 추가: PLC, 아두이노, 컨베이어, Unity, C++, ONNX, MES, 클라우드 배포

## 우선순위

일정이 밀리면 이 순서로 버린다:
WPF 이력 조회 화면 → Excel 요약 시트 → 실시간 바운딩 박스 → MVVM 리팩터링

**AI 파트(데이터 수집·라벨링·학습·실패 분석·재학습)는 줄이지 않는다.**

## 알려진 리스크

- 너트와 와셔가 탑뷰에서 시각적으로 구분되지 않을 수 있다 → 파일럿 촬영에서 최우선 확인
- SQL Server 연결: 혼합 모드 인증 + TCP/IP 활성화가 pyodbc 연결의 전제 조건
- 휴대폰으로 학습 → 웹캠으로 운영 시 도메인 갭. 최종 Test 셋에 웹캠 이미지를 충분히 포함
- Ultralytics는 AGPL-3.0. 개인 포트폴리오 공개는 문제없음

---

# 폴더 구조 (계획)

```
smart-factory-vision-inspection/
├── CLAUDE.md
├── README.md
├── .gitignore
├── docs/
│   ├── status.md           # 진행 상황 — 세션 시작 시 먼저 읽는다
│   ├── work-grades.md      # A/B/C 등급표
│   ├── foundations.md      # 기반 지식 — 각 기술이 무엇이고 어떻게 연결되는가
│   ├── decisions.md
│   ├── setup-log.md
│   ├── labeling-guide.md
│   ├── experiment-log.md
│   ├── daily-log.md
│   └── questions.md
├── ai-server/              # FastAPI + YOLO
├── wpf-client/
├── dataset/                # git 제외, Google Drive 관리
├── models/                 # git 제외
└── storage/                # git 제외
```
