# Windows(CRLF)와 git 저장소(LF) 사이 줄바꿈 차이로 생기는
# 경고와 불필요한 diff를 막는다. 텍스트 파일은 저장소에 LF로 저장한다.
* text=auto

# 아래는 텍스트로 변환하면 깨지는 파일이라 바이너리로 취급한다.
*.png binary
*.jpg binary
*.jpeg binary
*.pt  binary
*.onnx binary
*.xlsx binary
