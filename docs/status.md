# 진행 상황 (Status Board)

> **세션을 새로 시작하면 이 파일을 먼저 읽는다.** 지금 어디까지 왔고, 다음에 무엇을 하고, 무엇이 막혀 있는지만 담는다.
>
> 작성: Claude — **커밋할 때마다 함께 갱신한다.** 낡은 상태판은 없느니만 못하다.
> 판단과 근거는 여기 쓰지 않는다. `decisions.md`가 담당한다. 이 파일은 **포인터**다.
>
> 최종 갱신: **2026-09-18** (**#19 EXP-01 해석 완료.** 다음은 #20 추론·개수 스크립트 → test(s05) 한 번 → s07 어려운 조건 촬영)

---

## ▶ 지금 할 일 — 체크리스트

> **"뭐 해야 하지?" 싶으면 여기부터 본다.** 위에서부터 순서대로 하면 된다.
> 배경과 근거는 아래 1절, 판단은 `decisions.md`. **끝낸 줄은 `[x]`로 바꾼다.**

### ~~🏠 기숙사에서 — 웹캠 실측~~ → ✅ **완료 9/8. 7항목 전부 통과, F 확정 가능**

> 결과는 `decisions.md` **D-007 7-5**. 요약: 1:1 영역 **19.5~20.0cm**(높이 조정 불필요), 와셔 **64px**(목표 40px),
> 육각 모서리 **또렷함**. 카메라 앱은 **1:1로 저장하지 않으므로 크롭은 코드로 한다.**
> 같은 사진에서 레터박스는 8/6/6 오답(conf 0.775), 1:1 크롭은 **6/6/6 정답(conf 0.938)**.

### ~~📝 D-007 7절~~ → ✅ **완료 9/8. 웹캠 프레임 F 확정**

- 검사 영역 **20.0 × 20.0cm** (센서 전체 35.5cm를 중앙 1:1 크롭). 와셔 **64px**
- 크롭은 **촬영 후 코드로** 한다 (윈도우 카메라가 1:1 저장을 못 한다)
- **남은 아이폰 1세션도 20cm로 통일**한다. s01(23.2cm)만 예외로 남는다
- `sessions.csv`의 s01 `frame_width_cm`을 **22 → 23.2**로 고쳤다
- 뒤집는 조건을 프레임 폭 31cm → **와셔 40px 미만**(`imgsz` 640)으로 바꿨다

### 🔴 지금 할 일 — s02를 여는 일 (순서대로)

> **웹캠 세팅은 9/8에 20.0cm로 맞춰 검증했다. 삼각대를 건드리지 말 것.**

- [x] ~~**D-011 작성**~~ ✅ **완료 9/8.** 6세션 / 아이폰 2 + 웹캠 4 / Train 4·Val 1·Test 1
- [x] ~~`sessions.csv` s01 `frame_width_cm` 갱신~~ ✅ **완료 9/8.** 22 → 23.2
- [x] ~~**D-012 작성**~~ ✅ **완료 9/8.** 대책 세 축이 정해졌다 (아래 📷 촬영 절차)
- [x] ~~`sessions.csv`·`images.csv`에 s02 행 추가~~ ✅ **완료 9/8.** 검산 통과 — **세 부품 모두 0~6개가 각 5장씩**
- [x] ~~웹캠 4세션의 다양성을 어디서 줄 것인가~~ ✅ **완료 9/8. 조명 하나로 준다** (계획표는 D-007 2절)
- [x] ~~**📷 s02 촬영 35장**~~ ✅ **완료 9/9.** 34장 연속 + 마지막 장은 흐려서 34분 뒤 재촬영(9/11 EXIF로 정정) → `rename_session.py --apply`
- [x] ~~**s02 크롭**~~ ✅ **완료 9/10.** `scripts/crop_session.py` 작성 + 적용. 35장 전부 1080×1080
- [x] ~~**0. `start_labelstudio.ps1`이 안 켜짐**~~ ✅ **해결 9/10.** Smart App Control이 서명 없는 `label-studio.exe`를 차단 → 서명된 `python.exe`로 띄우게 바꿨다 (`setup-log.md` 5-2)
- [x] ~~**1. Label Studio 스토리지를 `dataset/crop`으로 추가**~~ ✅ **완료 9/10.** 스토리지 2개(`rename`=s01 35장, `crop`=s02 35장). DB로 확인: 잘리기 전 s02는 **0장**
  - **기존 `rename` 스토리지의 `Sync`는 앞으로도 누르지 않는다** — 16:9 원본 s02가 들어온다. 지우지도 않는다(s01 라벨 35장이 매여 있다)
  - s03~s05는 크롭 후 `dataset/crop`에 넣고 **`crop` 스토리지 `Sync`만** 한다
- [x] ~~**2. s02 라벨링 35장**~~ ✅ **완료 9/10.** 약 2.5시간 *(추정 — 서버 재시작 17:30, export 20:19)*. s01은 하루 걸렸다
- [x] ~~**3. `verify_counts.py`**~~ ✅ **완료 9/10. `검사완료 이상 없음` — s02 어긋남 0건.** s01은 16장이 틀렸었다. **D-012 대책이 통했다.** export는 `project-1-at-2026-09-10-20-19-...`(70개, s01 포함)
- [x] ~~스탠드 **밝기 조절이 되는지 확인**~~ ✅ **된다(9/11).** `sessions.csv`에 `stand_level` 열 추가 — s03 `high`, s04·s05 `low`, s06 `none`

### 🔴 지금 할 일 — 2차 학습 (첫 정식 실험)

> **⚠️ 9/13부터 원서 작성 병행.** 사용자 요청으로 A등급 코드도 *"Claude가 어떻게·왜를 설명하고 코드를 보여주면 사용자가 따라 치며 이해"* 방식으로 간다.
> D-001 뒤집는 조건 적용. 깊은 이해는 원서 뒤·면접 전. **AI 파트(#14 → 2차 학습 → #19 → #20 → #21) 완주가 최우선.**
> `decisions.md`에 이 변경을 한 줄 남길 것(사용자).

- [x] ~~`data.yaml` 갱신~~ ✅ 9/14 (`151308f`)
- [x] ~~Drive 업로드~~ ✅ 9/14 (사용자. 셀에서 140/35/35 확인)
- [x] ~~**2차 학습**~~ ✅ **9/14 `exp01_6sessions`.** 8.9분. 결과는 `runs/exp01_6sessions/`(로컬에도 복사)
- [x] ~~`experiment-log.md` 시작~~ ✅ **골격 작성(Claude).** 1·2절(설정·결과 사실)은 채웠고 **3·4절 판단은 `> Q.` 질문 — 사용자가 답한다**
- [x] ~~**#19 결과 해석**~~ ✅ **완료 9/18.** 사용자 답 → Claude 첨삭. 결정: D-004는 test까지 보고 / EXP-02는 설정이 아니라 **s07 어려운 조건 촬영** / **test(s05)는 지금 한 번 쓴다(안 A, D-011 변경 없음)**
- [ ] **#20 추론 스크립트** (A) — `best.pt`로 폴더의 사진마다 클래스별 개수를 내고 `images.csv`와 대조. **와셔→너트 3개가 어느 사진인지** 여기서 나온다. FastAPI 판정 로직의 원형
- [ ] **test(s05) 채점 딱 한 번** — #20 스크립트 또는 `model.val(split="test")`. 결과를 EXP-01 2절에 추가 → D-004 판정(val+test 210개 모두 5% 아래면 `해소`)
- [ ] **#21 실패 분석 1차** — 와셔→너트 3개(+test 오답)가 어느 사진·위치·배치인지 눈으로. EXP-01 4절에 추가
- [ ] **🏠 s07 어려운 조건 촬영 35장** — 겹침·가장자리·트레이 이동·자연광. D-007 2절 표에 행 추가, `images.csv`

### ~~🔴 #14 분할 스크립트~~ → ✅ **완료 9/14 새벽. `scripts/split_dataset.py`**

> 사용자가 코드, Claude가 주석. 라벨 210개를 기준으로 돌며 `verify_counts.py`의 `parse_label_filename`으로 이름을 정규화하고,
> `SPLIT_OF_SESSION` 표(**s01·s02·s03·s06 → train / s04 → val / s05 → test**)로 폴더를 정해 **이름을 `sXX_NNN`으로 다시 지어** 복사한다.
> 미리보기 → `--apply`, 출력 폴더가 비어 있지 않으면 멈춤(`crop_session.py`와 같은 구조).

- [x] ~~#14 작성~~ ✅ 9/6에 손으로 넣은 s01 35장은 지우고 스크립트로 다시 넣었다
- [x] ~~검증~~ ✅ **train 140 / val 35 / test 35.** 사진↔라벨 이름 짝 전부 일치. 클래스별 박스 합계 = `images.csv` 합계 — train bolt 420 / nut 452 / washer 452, val·test 각 105·105·105. **배경(빈 라벨) train 4 / val 1 / test 1** — 9/6에는 train에 0장이었다

### ~~🔴 s03~s06 라벨링~~ → ✅ **완료 9/13. 210장 전부 `검사완료 이상 없음`**

> 라벨링 시각은 Label Studio DB 기준(KST). s03 9/11 21:20~22:31(1시간 11분), s04 9/13 19:20~19:51(31분), s05 20:05~20:32(27분), s06 20:38~21:06(28분).
> s02가 2.5시간이었는데 s04~s06은 **30분 안팎**이다. export는 `project-1-at-2026-09-13-21-06-8725257a`(210개, 6세션 전부).

- [x] ~~**📷 s03·s04·s05 (웹캠) + s06 (아이폰) 촬영**~~ ✅ **완료 9/11 00:09~01:08.** 각 35장, 끊김 없음. 상세는 `daily-log.md` 9/11
- [x] ~~`sessions.csv`·`images.csv` s03~s06 행~~ ✅ **완료.** s03~s05는 사용자, s06은 Claude. 검산 통과
- [x] ~~rename → crop 4세션~~ ✅ **완료 9/11.** `crop/`에 s02~s06. s06은 정사각이라 복사만(4284)
- [x] ~~**D-007 2절 표 s06 조명 `미정` → `indirect`**~~ ✅ **완료 9/13** (사용자)
- [x] ~~**D-012 5절에 한 줄**~~ ✅ **완료 9/13** (사용자) — `s02_035` 재촬영, 11분 35초는 34장 기준
- [x] ~~**Label Studio `crop` 스토리지 Sync**~~ ✅ Sync 1회로 140장 전부 들어옴
- [x] ~~**라벨링 s03 → s04 → s05 → s06**~~ ✅ **완료 9/13.** 세션마다 export → `verify_counts.py` 0건 확인. 최종 **210장 어긋남 0건**

> ~~📌 s03~s05 찍을 때~~ ✅ 반영됨(9/11 확인: 4세션 마지막 장 모두 잘림 없음) — `s02_035`는 위쪽 여유가 **30픽셀(약 0.5cm)** 밖에 없었다.
> 세로로 세운 볼트 머리가 프레임 위 끝에 거의 닿았다. 반대로 오른쪽 아래는 넓게 비었다.
> **부품 덩어리를 조금 아래·오른쪽으로 옮긴다.**

> **⚠️ 삼각대 높이는 바꾸지 않는다.** 아래 3절에 *"세션마다 삼각대 높이나 각도를 조금씩 바꿀 것"* 이라고 적혀 있었는데,
> 9/8에 **프레임 폭 20cm 고정**으로 정하면서 **높이를 바꿀 수 없게 됐다.** 다양성은 **조명에서만** 준다.

> **s02는 웹캠으로 찍는다.** D-011에서 아이폰은 s01 포함 2세션뿐이고, 나머지 4세션이 웹캠이다.
> 웹캠 세팅이 9/8 그대로 살아 있으므로 웹캠부터가 자연스럽다.

### 📷 촬영 절차 (D-012) — ✅ 6세션 촬영 완료. 재촬영이 필요할 때만 본다

| | 규칙 |
|---|---|
| **1** | **부품 배치는 지금 방식 유지** — 앞 사진에서 더하고 뺀다. 매번 다 치우면 삼각대를 건드릴 위험이 있다 |
| **2** | **⭐ 매 장 찍기 전에 실물 개수를 센다.** 이것이 이번 대책의 전부다 |
| **3** | **되감기 지점 2곳을 특히 조심한다** — 블록 3→4에서 **와셔 6→0**, 블록 5→6에서 **너트 6→0**. 앞뒤가 전부 한 개씩이라 손이 5로 뺀다 |
| **4** | **트레이를 화면 정중앙에** — 1:1 크롭이라 여유가 없다 (D-007 Q49) |
| **5** | 예상 소요 **약 24분** (s01은 18분). 중간에 끊기면 **새 세션**이다 |

### 🟡 언제든 가능 — 밀리면 손해인 것

- [ ] **`daily-log.md` 읽고 사실과 다른 곳 고치기** — 8/13·8/17·8/18·8/19·8/23 + **9/6·9/7·9/8 + 9/9·9/10·9/11·9/13(새로 씀. 9/11은 스탠드 거리가 추정. 9/13은 사용자 확인 완료)** *(8/18부터 다섯 칸 전부 Claude가 쓴다. **읽고 고치는 과정을 생략하면 이 방식은 실패한다**)*
- [x] ~~**D-012 5절·6절에 결과 한 줄씩**~~ ✅ **완료 9/10.** 실제 11분 35초(삼각대 덕), `verify_counts.py` 0건
- [x] ~~**D-009 8·9·10절**~~ ✅ **완료 9/9.** 규칙 변경 이력 표도 만들었다(10절). **`decisions.md`에 미작성 질문이 0개가 됐다**
- [ ] D-007에 촬영 포맷(JPEG) 한 줄 추가
- [ ] D-008 6절에 *"합계 18에서는 밀착이 강제된다"* 한 줄 추가

---

## 0. 한 줄 요약

**⭐ s01 데이터가 완성됐다(9/3). `검사완료 이상 없음` — 계획과 실제가 완전히 일치한다.**
`verify_counts.py`가 찾은 **24건 중 23건은 `images.csv`가, 1건은 라벨이 틀렸다.** 둘 다 고쳤다.
**`images.csv`를 이제 실패 분석의 채점표로 쓸 수 있다.** 8/18 이후 처음이다.

**⭐⭐⭐ 9/14 — EXP-01, 첫 정식 학습. 처음으로 믿을 수 있는 숫자가 나왔다.**
train 140(s01·s02·s03·s06) / val 35(s04). val은 train에 없는 조명 조합인데 **mAP50 0.994, mAP50-95 0.911.**
혼동 행렬: 315개 중 312 정답, **틀린 것은 와셔 3개를 너트로 본 것뿐**(2.9%). 놓침 0, 헛것 0. D-004의 5% 기준 아래다.
`experiment-log.md`를 시작했다. 설정·결과는 채웠고 **해석·가설은 사용자가 `> Q.`에 답한다.** test(s05)는 아직 안 썼다.

**⭐ 9/14 새벽 — #14 `split_dataset.py` 완료. `result_data/`가 6세션 기준으로 채워졌다.**
train 140(s01·s02·s03·s06) / val 35(s04) / test 35(s05). 박스 합계가 `images.csv`와 전부 일치. 배경 이미지가 train에 4장 들어갔다(9/6에는 0장).
**9/13부터 원서 작성 병행** — A등급 코드를 "설명+코드 제시 → 사용자가 따라 침" 방식으로 바꿨다(D-001 뒤집는 조건). 다음은 `data.yaml` → Drive → 2차 학습.

**⭐⭐⭐ 9/13 밤 — 라벨링이 끝났다. 210장 전부 `검사완료 이상 없음`.**
s03(9/11 저녁, 1시간 11분) → s04·s05·s06(9/13 저녁, 각 30분 안팎). s01이 하루, s02가 2.5시간이었으니 손이 다섯 배 빨라졌다.
6세션 어느 것도 `images.csv`와 어긋나지 않았다. **데이터 파트(#8~#15)가 끝났고, 남은 것은 #14 분할 스크립트부터 AI 파트다.**

**⭐⭐⭐ 9/11 새벽 — 촬영이 끝났다. 6세션 210장.**
s03·s04·s05(웹캠, 조명 3종) + s06(아이폰, 폰 홀더 거치, 20cm)을 한 밤에 찍었다. 각 8~10분, 끊김 없음.
이름 변경·크롭까지 끝나 `crop/`에 s02~s06이 있다. **남은 것은 라벨링 140장과 분할·학습이다.**

**⭐⭐⭐ 9/10 밤 — s02 데이터가 완성됐다. `verify_counts.py` 0건.**
s01은 35장 중 16장이 계획과 달랐는데, s02는 **35장 전부 일치**했다. D-012의 대책(매 장 찍기 전 실물 세기)이 통했다.
라벨링은 약 2.5시간(추정)으로 s01(하루)보다 훨씬 빨랐다. 도중에 Smart App Control이 Label Studio 실행을 막아 스크립트를 고쳤다(`setup-log.md` 5-2).

**⭐⭐⭐ 9/9~9/10 — s02를 찍었다. 두 번째 세션이자 첫 웹캠·첫 삼각대 세션이다.**
`decisions.md`에 **답을 안 쓴 질문이 0개**가 됐다(D-001~D-012). s02를 막고 있던 결정이 전부 풀렸다.
촬영은 34장까지 **10분 43초**, 마지막 장은 흐려서 34분 뒤 다시 찍었다(9/11 정정). 되감기 지점(`s02_012→013`, 와셔 6→0)에서 **62초**로 손이 멈춘 흔적이 EXIF에 남았다.
크롭 단계(`crop_session.py`)를 새로 만들어 35장을 **1080×1080**으로 잘랐다.
~~아직 라벨링 전이라 수량 오류 대책이 통했는지는 모른다~~ → **9/10 밤 0건으로 확인됐다.**

**⭐⭐ 9/6 — 첫 학습을 돌렸다. 배관이 뚫렸다.**
Colab T4에서 `yolo11n` 100 epoch, **4.5분**. 에러 없이 완주하고 `best.pt`가 Drive에 남았다.
**val 인스턴스 63개가 `images.csv`의 val 합계(21·21·21)와 정확히 일치** — 경로·짝맞춤이 전부 걸렸다는 뜻이다.
**⚠️ 성능 숫자는 믿지 않는다**(train·val이 같은 세션). 판단은 딱 하나, "배관이 뚫렸다"뿐이다.
결과 요약은 1절 아래, 원본은 `runs/s01_pipe_check/`(git 제외).

**⭐⭐⭐ 9/8 — 웹캠 프레임이 풀렸다. 선택지 F 확정 가능.**
기숙사 실측 7항목이 **전부 통과**했다. 1:1 크롭 영역이 **정확히 20.0cm**라 **카메라 높이를 올릴 필요조차 없었고**,
와셔가 **64px**(목표 40px)로 찍힌다. 같은 사진에서 **레터박스는 8/6/6 오답(conf 0.775), 1:1 크롭은 6/6/6 정답(conf 0.938)**.
**크롭이 실제로 탐지 결과를 바꾼다는 것이 확인됐다.** 근거는 D-007 **7-5**.

~~s02를 막고 있는 것은 이제 결정 두 개뿐이다 — D-011과 D-012.~~ → **9/9 둘 다 작성 완료. 9/11 촬영까지 전부 끝났다.**

계획보다 약 **3주 밀린 상태**다. D-011은 그 지연에 대한 대응이다.

---

## 1. 다음에 할 일 (순서대로)

| 순서 | 할 일 | 등급 | 막고 있는 것 |
|---|---|---|---|
| ~~–~~ | ~~Label Studio 세팅~~ / ~~#12 s01 라벨링~~ | C / A | ✅ **둘 다 완료 8/24** |
| ~~–~~ | ~~`scripts/verify_counts.py` 작성~~ | A | ✅ **완료 9/2.** 주석까지 |
| ~~–~~ | ~~`images.csv` 수정~~ / ~~`labeling-guide.md` 5·6절~~ | A | ✅ **둘 다 완료 9/3** |
| ~~–~~ | ~~`result_data/` 배치~~ / ~~`data.yaml` 작성(#16)~~ / ~~Drive 업로드~~ | C / B | ✅ **전부 완료 9/6** |
| ~~–~~ | ~~학습 스크립트 + 첫 학습(#17)~~ | A | ✅ **완료 9/6.** `scripts/train.py`. 코드는 사용자 요청으로 Claude가 함께 작성(아래) |
| ~~–~~ | ~~`decisions.md` D-010 작성~~ / ~~D-004 리스크 판단~~ | A | ✅ **완료 9/6.** 2라운드 첨삭. D-004는 **`판단 보류`**로 기록 |
| ~~–~~ | ~~**웹캠 프레임 결정** — D-007 **7절** Q44~Q56~~ | A | ✅ **9/8 실측 7항목 전부 통과 → F 확정 가능.** Q50~Q53만 D-011에 막혀 남았다 |
| ~~–~~ | ~~D-011 작성~~ / ~~D-012 작성~~ / ~~D-009 8·9·10절~~ | A | ✅ **전부 완료 9/9** |
| ~~–~~ | ~~s02 촬영·크롭·라벨링~~ | A / B / A | ✅ **완료 9/9~9/10.** `verify_counts.py` 0건 |
| ~~–~~ | ~~s03·s04·s05·s06 촬영 🏠 → rename → crop~~ | A / C | ✅ **완료 9/11 새벽.** 6세션 210장 |
| ~~–~~ | ~~s03 → s04 → s05 → s06 라벨링~~ / ~~D-007·D-012 두 줄~~ | A | ✅ **완료 9/13.** 210장 `verify_counts.py` 0건 |
| ~~–~~ | ~~#14 분할 스크립트~~ | A | ✅ **완료 9/14.** `split_dataset.py`. 140 / 35 / 35, 박스 합계 `images.csv`와 일치 |
| ~~–~~ | ~~`data.yaml` → Drive → **EXP-01 학습**~~ | B / C / A | ✅ **완료 9/14.** val mAP50-95 0.911, 와셔→너트 3 |
| ~~–~~ | ~~#19 결과 해석~~ | A | ✅ **완료 9/18** |
| **1** | **#20 추론 스크립트** — `best.pt`로 폴더 사진마다 클래스별 개수 → `images.csv` 대조 → 틀린 사진 목록. FastAPI 판정 로직의 원형 | **A** | 없음 (1번과 병행 가능) |
| 2 | **test(s05) 한 번** → D-004 판정 | A | 1번 |
| 3 | **#21 실패 분석 1차** — 와셔→너트 3개 + test 오답의 사진·위치·배치 | **A(최상위)** | 1·2번 |
| 4 | **🏠 s07 어려운 조건 촬영·라벨링** → EXP-02(데이터만 바꿈) → #21 2차 → #22 | A | 3번 |
| 5 | **AI 파트 이후** — FastAPI(추론 + OK/NG) → MSSQL 저장 → Excel → WPF. 밀리면 버리는 순서는 CLAUDE.md | B / C | 2번 |

**⭐ 첫 학습 결과 — 9/6** (D-010 재료. `runs/s01_pipe_check/`는 git 제외라 여기 요약을 남긴다)

`yolo11n.pt` / `epochs=100` / `imgsz=640` / `batch=16` / Colab T4 / **268초(4.5분)**

| 지표 | 값 | |
|---|---|---|
| Precision | 0.995 | 찾았다고 한 것 중 진짜 비율 |
| Recall | 0.983 | 실제 있는 것 중 찾아낸 비율. **개수를 세는 우리 프로젝트에서는 이쪽이 더 중요** |
| mAP50 | 0.985 | 물건을 찾았는가 |
| mAP50-95 | 0.885 | 박스를 얼마나 정밀하게 그렸는가 |

**혼동 행렬 — `bolt` 20/21, `nut` 21/21, `washer` 21/21. 볼트 1개 미탐지, 오분류 0건.**

- **⭐ 너트↔와셔 오분류 0%** — D-004의 최대 리스크였다. 뒤집는 조건이 5%인데 0%다. **단 아이폰·한 세션·`imgsz` 640 조건에서다**
- **볼트 1개 미탐지**(21개 중 1개, 4.8%). `val_batch0_pred.jpg`를 보면 `s01_035`(18개 밀착)가 압도적으로 복잡하다. **거기일 가능성이 높지만 확인은 안 했다 (추정)** — #21 실패 분석에서 짚을 것
- **빈 트레이 `s01_029`에 박스 0개.** 헛것을 보지 않았다
- **1~10 epoch 구간에서 `cls_loss`가 3.5에서 꿈쩍도 안 했다.** mAP 0.03. 20~40 사이에서 갑자기 풀렸다. **10 epoch에서 멈췄으면 코드가 틀린 줄 알았을 것이다**
- **과적합이 안 왔다** — `val_loss`가 100 epoch에서 최저였다. 모델이 훌륭해서가 아니라 **val이 train과 사실상 같은 데이터라 과적합해도 val 점수가 안 떨어지는 것**이다. D-003이 왜 있는지가 여기서 드러난다

**판단은 D-010에 기록했다(9/6).** D-004의 너트↔와셔 리스크는 **`판단 보류`** — 0%가 성립한 조건이 아이폰·1세션·`imgsz` 640·손 촬영이고 **표본이 42개 인스턴스뿐**이기 때문이다. `해소`로 바꾸는 기준은 **웹캠 사진이 val/test에 들어간 상태에서 5% 이하**로 정했다(D-010 6절).

**⚠️ 이 숫자를 성능으로 읽지 말 것.** train 28장과 val 7장이 같은 조명·같은 높이·같은 손·같은 나뭇결이다. 모델이 배운 것 중 얼마가 "볼트란 무엇인가"이고 얼마가 "이 갈색 나무 위 은색 막대"인지 구분할 방법이 없다. **`experiment-log.md`에 정식 실험으로 남기지 않는다.**

**⭐ 분할 결과 — 9/6 확정** (D-010 재료)

`s01_001`~`s01_028`을 **train 28장**, `s01_029`~`s01_035`를 **val 7장**으로 나눴다. test는 0장(`data.yaml`에서 생략).

- **왜 뒤 7장인가** — `images.csv`를 보면 001~028은 4장씩 7블록이고 블록 안 4장은 개수·배치가 **완전히 동일**하다. 거기서 1장을 val로 빼면 train에 쌍둥이 3장이 남아 val이 답안지가 된다. 반면 029~035는 `0,0,0`부터 `6,6,6`까지 **7장이 전부 다르고 train에 쌍둥이가 없다.** val 박스 수가 0·3·6·9·12·15·18로 떨어지는 것이 그 증거다
- **⚠️ 그 결과 train에 배경 이미지가 0장이다.** 빈 트레이(`s01_029`)가 s01의 유일한 배경 이미지인데 val로 갔다. D-005의 뒤집는 조건(*"아무것도 탐지 못 하는 비율 10% 이상"*)을 볼 때, 이번 학습에는 그 재료가 **아예 없었다**는 것을 전제로 판단해야 한다
- **검증 통과** — 사진↔라벨 이름 35쌍 전부 일치, 클래스별 박스 합계가 `images.csv`와 일치(bolt 105 / nut 137 / washer 137)
- **`data.yaml`은 프로젝트 최상위에 둔다** — `dataset/`이 `.gitignore` 대상이라 그 안에 넣으면 git에 안 남는다. `metadata/`를 밖에 둔 것과 같은 이유다

**⭐ 첫 학습은 "배관 점검"이다 — 9/3 결정**

6주 계획의 4주차인데 **학습을 한 번도 돌리지 못했다.** 그래서 s02를 기다리지 않고 **s01 35장만으로 먼저 한 바퀴 돌린다.**

- **얻는 것** — 파이프라인이 뚫린다. `data.yaml` 경로, Colab에 데이터 올리는 법, 학습 명령, 결과 파일 위치를 **한 번 겪어두면** s02~s10을 다 찍은 뒤에 처음 부딪히지 않는다. 여기서 막히는 것들(경로 오타, Drive 마운트, 폴더 구조)은 데이터가 350장이어도 똑같이 막힌다
- **감수할 것** — 35장으로는 성능이 안 나온다. Val/Test를 나눌 세션이 하나뿐이라 **D-003(세션 단위 분할)을 지킬 수 없다.** Train과 Val이 사실상 같아져서 숫자가 나와도 의미가 없다
- **⚠️ 그래서 이 결과를 `experiment-log.md`의 정식 실험으로 남기지 않는다.** 성능 측정이 아니라 **배관 점검**으로만 쓴다. 정식 1차 실험은 350장이 모인 뒤다
- **#14 분할 스크립트는 이번엔 건너뛴다** — 세션이 하나뿐이라 "세션 단위로 나눈다"는 핵심 로직을 시험할 수가 없다. 35장을 손으로 폴더에 넣고, **s02~s10이 모인 뒤에 제대로 만든다**
- **`decisions.md` D-010으로 기록할 것** (아직 안 씀)

**순서와 무관하게 언제든 가능한 것**

- **`daily-log.md` 8/13·8/17·8/18·8/19·8/23 읽고 사실과 다른 곳 고치기** — 8/18부터 다섯 칸 전부 Claude가 쓴다. **읽고 고치는 과정을 생략하면 이 방식은 실패한다** (`work-grades.md` 8절)
- **D-007에 촬영 포맷 한 줄 추가** — 8/17에 아이폰을 JPEG(`높은 호환성`)로 바꿨는데 원본 문서에 기록이 없다. 아래 6절 표에만 사본이 있는 상태다
- D-008 6절에 *"합계 18에서는 밀착이 강제된다"* 한 줄 추가 (계획표 마지막 행을 `tight`로 바꾼 근거)
- `decisions.md` D-010~ 후보 — FastAPI 단일 프로세스 / C#·WPF 선택. 3주차 이후라 급하지 않다

**D-009 9절(감수하는 것) 재료** — 촬영 후에 쓸 때 꺼내 쓸 것

- `images.csv`에서 **같은 개수로 찍는 4~5장은 행이 완전히 동일하다** (예: `s01_001`~`s01_004` 전부 `0,2,4,tight`). 부품을 실제로 어디에 놓았는지는 기록되지 않으므로, 실패 분석에서 *"같은 조건인데 왜 한 장만 틀렸나"* 를 CSV로는 답할 수 없다. 배치를 좌표까지 적으면 촬영 중에 기록할 일이 생기고, 손 촬영에서 그건 세션이 끊길 위험이라 감수한 것이다

---

## 2. 절대 하지 말 것 (반복 확인용)

- **계획표 순서를 건너뛰고 촬영 금지** — 이름 변경 스크립트가 촬영 시각 순으로 `001`부터 번호를 붙인다. 순서가 어긋나면 `images.csv`의 개수와 실제 사진이 어긋나고, 라벨링 검수가 전부 무의미해진다
- **촬영 도중 중단하면 새 세션** (D-009 Q4·Q5) — 손 촬영이라 다시 앉으면 높이·각도가 달라진다. **삼각대가 생겨도 이 규칙은 당분간 그대로 둔다.** 규칙의 근거가 약해진 것은 맞지만, 푸는 것은 D-009 10절에 근거를 쓰고 나서 할 일이다
- **프레임 폭 측정용 사진을 데이터에 남기지 말 것** — 자를 얹고 찍은 사진이 남으면 이름 변경 스크립트가 그것을 `001`번으로 잡는다. 눈금을 읽었으면 바로 지운다
- ~~**⭐ Label Studio 클래스 등록 순서**~~ → **8/24 확인 완료.** YOLO로 내보낸 `classes.txt`·`notes.json`이 `0=bolt, 1=nut, 2=washer`로 나오는 것을 파일로 확인했다(`setup-log.md` 5-1). 대신 **라벨링 설정(XML)의 라벨 순서를 앞으로 바꾸지 말 것** — 그 순서가 곧 클래스 번호다
- **⭐ Label Studio를 `label-studio start` 로 그냥 켜지 말 것** — 반드시 **`.\scripts\start_labelstudio.ps1`**. 환경 변수는 그 프로세스에만 붙으므로, 그냥 켜면 로컬 파일 서빙이 꺼져 **사진이 전부 깨져 보인다.** 증상만 보고는 원인을 찾기 어렵다
- **단축키 1·2·3은 클래스 번호가 아니다** — 손가락 편하라고 준 키일 뿐이다. 클래스 번호는 `0, 1, 2`. 박스를 그릴 때 **키를 믿지 말고 화면에 켜진 라벨 이름을 볼 것**
- ~~s01_001에 시험용 라벨이 남아 있다~~ → **8/24 지우고 다시 그렸다**
- **⭐ s02부터는 계획표의 되감기 지점(6→0)을 조심할 것** — s01에서 정확히 여기서 틀렸다. 앞뒤가 전부 "한 개씩 빼기"인데 되감기만 "6개를 한꺼번에 빼기"라 손의 리듬이 끊긴다. → **대책은 D-012에서 정했다(9/8). 매 장 찍기 전에 실물 개수를 센다**
- ~~`images.csv`의 s01 행은 아직 계획값이다~~ → **9/3 실제 값으로 수정 완료.** 이제 채점표로 써도 된다
- **⭐ 라벨링 중 빈 클릭에 주의할 것** — `s01_016`에 **점 크기 박스**가 하나 생겨 있었다. 눈에 잘 안 띈다. 사진을 넘기기 전에 **박스 목록의 개수를 세는 것**이 유일한 방어다 (`labeling-guide.md` 5절)
- **`verify_counts.py`를 만능으로 믿지 말 것** — **개수만 본다.** 빠뜨린 박스 하나와 잘못 만든 박스 하나가 같은 사진에서 겹치면 개수가 맞아 통과한다. `s01_016`은 점 박스만 있었기에 걸린 것이다
- **`verify_counts.py`가 `images.csv`를 자동으로 고치게 만들지 말 것** — 어긋남이 CSV 탓인지 라벨 탓인지는 사진을 봐야 안다. **찾는 도구와 고치는 도구는 분리한다.** 자동으로 고치는 스크립트는 잘못 짰을 때 원본을 통째로 망가뜨리고 되돌릴 수도 없다
- **`dataset/export/`에 export를 두 벌 이상 쌓지 말 것** — `LABEL_PATH`가 `export`까지만 잡고 `*/labels/*.txt`로 훑기 때문에 같은 이름이 두 번 걸린다. 지금은 중복을 만나면 스크립트가 멈추게 해뒀다. 다시 내보낼 때는 **이전 폴더를 지우거나 옮긴다**
- ~~`labeling-guide.md` 3절 없이 라벨링 시작 금지~~ → **3절 작성 완료(8/23).** 대신 라벨링 중 규칙을 바꾸면 **반드시 8절 표에 기록**한다. 안 남기면 한 데이터셋에 두 규칙이 섞인다
- **⭐ 크롭하기 전에 라벨링하지 말 것** — 라벨을 그린 뒤에 자르면 잘라낸 만큼 그림이 밀려 **박스 좌표가 전부 어긋난다.** 순서는 `origin → rename → crop → 라벨링`이다. 웹캠 세션에만 해당한다(아이폰은 1:1로 저장되어 크롭이 필요 없다)
- **`metadata/`를 `dataset/` 안으로 옮기지 말 것** — `.gitignore`가 `dataset/`을 통째로 제외한다. CSV는 실패 분석의 유일한 근거라 git에 남아야 한다
- 이미지 / `.pt` 모델 / 데이터셋을 git에 커밋 금지
- Train/Val/Test를 랜덤 셔플로 분할 금지 — **반드시 촬영 세션 단위** (D-003)

---

## 3. 지금 막혀 있는 것

| 제약 | 영향 | 언제 풀리나 |
|---|---|---|
| ~~웹캠 프레임을 정해야 한다~~ | **✅ 9/8 완전 해소.** F(중앙 1:1 크롭), 검사 영역 **20.0 × 20.0cm**, 와셔 **64px**. D-007 2절 표까지 반영 완료 | — |
| ~~⭐ 크롭은 촬영 규칙과 한 쌍이다~~ | **✅ 9/11 해소.** s02~s05 웹캠 4세션 모두 중앙 배치로 찍어 크롭 후 잘린 부품 없음(마지막 장 4장 눈으로 확인). 다만 `s02_035` 위 30px, `s04_035` 오른쪽 2px로 여유가 거의 없었다 | — |
| ~~웹캠 세로가 트레이보다 좁다~~ | **9/7에 완화됐다.** 세로가 17.5가 아니라 **19.4cm**였다(측정면 오차). 트레이 짧은 변 20cm에 거의 닿는다 | F를 택하면 정확히 20cm가 되어 해소된다 |
| ~~`frame_width_cm`이 실측이 아니다~~ | **측정 완료.** s01 = **약 23.2cm**(와셔 역산 22.7cm에 보정계수 1.023 적용). 사진마다 **21.9~24.9cm로 흔들렸다** | `sessions.csv`에 무엇을 적을지는 **D-007 Q51**에서 정한다 |

**⭐ 9/7에 새로 안 것 — 측정면 오차와 손 촬영의 편차**

**① 프레임 폭은 부품이 놓이는 면에서 재야 한다.** 8/19에 잰 31.5cm는 자를 **모서리 벽 상단**에 얹은 값이고, 부품은 **트레이 바닥**에 놓인다. 바닥은 카메라에서 더 멀어 더 넓게 찍힌다. 부품 면에서 다시 재니 **34.5cm — 9.5% 오차**였다. 자세한 것은 **D-007 7-0**.

> **앞으로 자는 트레이 바닥에 놓는다.** 모서리 벽에 얹지 않는다.

**② s01은 단일 조건이 아니다.** 와셔 라벨 137개로 사진별 프레임 폭을 역산하니 **21.4 ~ 24.3cm(12.7% 편차)** 였고, 세션 초반에 높고 중반에 안정되다가 마지막에 다시 높아지는 **흐름**이 있었다. 손 촬영의 비용이 처음으로 숫자가 됐다. **D-009 9절 재료다.**

**③ 도메인 갭 첫 신호(측정 아님).** `best.pt`(아이폰만 학습)를 웹캠 테스트 사진 6장에 돌리니 볼트·너트는 6개씩 안정적으로 잡았는데 **와셔는 2장에서 3개·4개만** 잡았다. 웹캠에서 와셔가 36px로 작아진 것과 방향이 맞다. **정답 개수를 확인한 게 아니라 신호일 뿐이다.**

**✅ 9/3에 풀린 것 — s01 수량 오류**

계획표는 세 부품이 각자 다른 지점에서 **6→0으로 되감기는** 구조인데, 손은 "한 개씩 뺀다"는 리듬을 따라 **6 다음에 5로 계속 뺐다.** 너트(블록 6부터)·와셔(블록 4부터)가 어긋났고 **볼트는 6이 마지막 블록이라 무사**했다. **35장 중 16장, 총 24건.**

**24건 중 23건은 `images.csv`가 틀린 것이었고, 1건은 라벨이 틀린 것**이었다(`s01_016`의 점 크기 박스). 둘 다 고쳤고 `검사완료 이상 없음`을 확인했다. **s01은 폐기하지 않았다** — 사진 자체는 정상 데이터였고 틀린 것은 기록이었다.

**s01 최종 수량 분포** (계획은 세 부품 모두 각 개수 5장이었다. 지금은 `images.csv`가 아래 값과 일치한다)

| 개수 | 0 | 1 | 2 | 3 | 4 | 5 | 6 |
|---|---|---|---|---|---|---|---|
| `bolt` | 5 | 5 | 5 | 5 | 5 | 5 | 5 |
| `nut` | 1 | 1 | 5 | 5 | **9** | **9** | 5 |
| `washer` | 1 | 1 | 5 | 5 | **9** | **9** | 5 |

> **이 분포 편중은 s01에 그대로 남아 있다.** 기록만 고친 것이지 사진을 다시 찍은 게 아니다.
> 너트·와셔의 4개·5개가 9장씩이고 0개·1개가 1장씩이다. **세션 10개 중 하나의 편중이라 전체 350장에서 희석된다**는 것이 폐기하지 않은 근거였다. 학습 후 s01에서만 특정 개수의 성능이 이상하면 이것을 먼저 의심한다.
>
> `washer`의 5개가 8장, 6개가 6장이었던 것은 **`s01_016`의 점 박스 때문**이었다. 그 박스를 지우니 너트와 같은 분포가 됐다. 8/24에 눈으로 센 기록이 틀린 게 아니라, **라벨이 틀려 있었던 것이다.**

**8/17~8/19에 풀린 것**

- ~~길이 측정 도구가 없다~~ → 50cm 자 구매(8/17)
- ~~부품 18개가 프레임에 들어가는지 미확인~~ → **들어간다.** D-008의 정상 3개·상한 6개 유지
- ~~거치 기구가 없다~~ → **삼각대 도착(8/19).** s01은 손 촬영(`mount=hand`), s02부터 `tripod`
- ~~카메라 높이·배율이 불확실하다~~ → EXIF로 확정: **1x(24mm 환산), 정사각 1:1, 4284×4284.** 높이는 트레이 윗면 기준 22cm
- ~~웹캠 미도착~~ → **레이저 Kiyo X 도착(8/19).** 1920×1080, 16:9
- ~~웹캠 최소초점거리 미확인~~ → **초점 맞는다(8/19 테스트).** 이 리스크는 사라졌고, 대신 위의 프레임 폭 문제가 남았다

**삼각대가 생기면서 새로 신경 쓸 것**

- ~~세션마다 삼각대 높이나 각도를 일부러 조금씩 바꿀 것~~ → **9/8에 프레임 20cm 고정으로 바뀌어 높이를 못 바꾼다. 다양성은 조명(+스탠드 밝기)에서만 줬다.** 원문: 삼각대를 고정해두고 7세션을 연달아 찍으면 **세션끼리 조건이 거의 같아진다.** D-003이 세션 단위로 나누는 이유가 세션마다 조건이 다르기 때문인데, 그게 사라지면 Val/Test가 Train과 사실상 같아져 **성능이 실제보다 좋게 나온다.** 세션마다 삼각대 높이나 각도를 일부러 조금씩 바꿀 것
- **s01이 유일한 손 촬영 세션**이다. 버리지 말 것

---

## 4. work-grades 항목별 상태

| # | 작업 | 등급 | 상태 |
|---|---|---|---|
| 1~7 | 환경 구축 (git, 가상환경, SQL Server, Colab, Label Studio, .NET, 폴더) | C | ✅ `setup-log.md`. **5번(Label Studio)은 프로젝트 세팅까지 완료 — 5-1절(8/24)** |
| 8 | 촬영 환경 설계 | A | ✅ D-007 (`잠정`) |
| 9 | 파일럿 촬영 + 너트/와셔 구분 판단 | A | ✅ D-007 4절 → **D-004 확정** |
| 10 | 파일명·세션 명명 규칙 | A | ✅ **D-009** (`잠정`, `[촬영 후]` 3절 미작성) |
| 11 | 라벨링 규칙 (`labeling-guide.md`) | A | ✅ **1~8절 완료.** **5·6절은 9/3에 Claude가 작성**(도구 동작 기록이라 판단이 아니라는 선에서). 7절 등급 판단 정정과 근거 숫자도 9/3 |
| 12 | 라벨링 실작업 | A | ✅ **s01(8/24)·s02(9/10) 완료.** export는 `project-1-at-2026-09-10-...`(70개). ⬜ **s03~s06 140장 — 9/11 촬영 완료, 라벨링 대기** |
| 13 | Train/Val/Test 분할 기준 | A | ✅ D-003 + D-011·D-007 2절로 배정 확정 — **Train s01·s02·s03·s06 / Val s04 / Test s05** |
| – | **s01 → `result_data/` 배치** (#14 대신 일회성 복사) | C | ✅ **완료 9/6.** 세션이 하나뿐이라 분할 스크립트는 건너뛰고 손으로 넣었다 |
| – | **`scripts/verify_counts.py`** (#14의 예행연습) | A | ✅ **완료 9/2.** 라벨 파일명 정규화 · `glob` 훑기 · 이름을 키로 잇기를 여기서 먼저 했다. **#14는 이 셋을 그대로 다시 쓴다** |
| – | **`scripts/crop_session.py`** (웹캠 16:9 → 1:1 중앙 크롭) | B | ✅ **완료 9/10.** 등급표에 없던 항목. D-007 F를 실제로 적용하는 단계다 |
| 14 | 분할 실행 스크립트 | A | ⬜ 파일명 정규화는 `verify_counts.py`의 `parse_label_filename`을 가져다 쓰면 된다 |
| 15 | 촬영 메타데이터 CSV | A | ✅ **6세션 전부 작성(9/11).** `sessions.csv` 6행(+`stand_level` 열), `images.csv` 210행. s01·s02는 라벨과 일치 확인, s03~s06은 라벨링 후 대조 |
| 16 | `data.yaml` | B | ✅ **완료 9/6.** 최상위 `data.yaml`. `path`는 Colab 절대경로, `test`는 주석 처리 |
| 17 | 학습 스크립트 | A | ✅ **완료 9/6.** `scripts/train.py`. **사용자가 "처음이라 감이 안 잡힌다, 이번 한 번은 끝까지 가르쳐 달라"고 명시 요청해 Claude가 코드까지 작성했다.** A등급 4단계 힌트에 해당한다. **다음 학습 스크립트부터는 다시 사용자가 쓴다** |
| 18 | Colab 노트북 세팅 | C | ✅ **완료 9/6.** GPU·마운트에 더해 `result_data`(112MB)와 `data.yaml`을 Drive `smart-factory-vision/`에 업로드, 경로 확인까지 |
| 19 | 학습 결과 해석 | A | ✅ **완료 9/6.** 지표 해설 + 판단을 **D-010 4·5·6절**에 기록. 5절(과적합이 안 온 것이 아니라 안 보이는 것)이 핵심 |
| 20 | 추론·시각화 스크립트 | A | ⬜ |
| 21 | 실패 사례 분석 + 개선 가설 | A(최상위) | ⬜ |
| 22 | 실험 로그 기록 | A | ⬜ |

---

## 5. 결정 기록 상태

| | 내용 | 상태 |
|---|---|---|
| D-001 | A/B/C 작업 등급 | 확정 |
| D-002 | 학습은 Colab에서 | 확정 |
| D-003 | Train/Val/Test를 촬영 세션 단위로 분할 | **잠정** (실제 촬영 전) |
| D-004 | 검사 대상 볼트·너트·와셔 3종 | 확정 |
| D-005 | 빈 트레이는 클래스가 아니라 배경 이미지 | 확정 |
| D-006 | DB는 MSSQL | 확정 |
| D-007 | 촬영 환경 고정 | **잠정** (본 촬영 전) |
| D-008 | 정상 수량 기준과 데이터의 수량 다양성 | **잠정** (본 촬영 전) |
| D-009 | 촬영 세션 정의와 파일명 규칙 | **잠정** — **1~10절 전부 작성 완료(9/9).** 상태를 `확정`으로 올릴지는 미정 |
| D-010 | 한 세션뿐일 때 첫 학습을 돌릴 것인가 | **1회성** (9/6 작성 완료) |
| D-011 | 촬영 규모를 10 → 6세션으로 줄이고 기기 비율을 뒤집을 것인가 | **잠정** (9/8 작성 완료) |
| D-012 | s02부터 촬영 수량 오류를 어떻게 막을 것인가 | **확정** (9/8 작성 완료) |

---

## 6. 자주 찾는 확정 수치

한 곳에서 보기 위한 사본이다. **원본이 아니다** — 어긋나면 괄호 안의 문서가 맞다.

**클래스** (`labeling-guide.md` 1-1 표가 원본. `data.yaml`은 여기서 옮겨 적는다)

| 번호 | 이름 |
|---|---|
| 0 | `bolt` |
| 1 | `nut` |
| 2 | `washer` |

**부품 치수** — 상품 페이지 규격이며 실측이 아니다 (D-007 0절)

| 부품 | 치수 |
|---|---|
| 볼트 | 머리 대변 15.9mm / 대각 18.4mm, 머리 두께 8.2mm, 기둥 M10 × 100mm |
| 너트 | 대변 15.9mm / 대각 18.4mm, 내경 M10 |
| 와셔 | 외경 19.6mm, 내경 M10 |

- 볼트 머리와 너트는 탑뷰에서 크기·모양이 같다 → 구분 단서는 **중앙 구멍 유무** 하나뿐. 그래서 **볼트는 눕혀서 배치**한다
- 너트와 와셔의 탑뷰 최대 폭 차이는 **약 6%** → 크기만으로 판단 금지. 단서는 **외곽선이 육각이냐 원이냐**

**촬영** (D-007)

| 항목 | 값 |
|---|---|
| 각도 / 프레임 | 탑뷰, 20cm × 20cm |
| **웹캠 프레임 폭** | 센서 전체 **35.5cm** → **중앙 1:1 크롭 후 20.0 × 20.0cm** (9/8 실측. **선택지 F**) |
| **크롭** | **코드로 자른다.** 윈도우 카메라 앱이 1920×1080으로만 저장한다 (D-007 Q47 = ②) |
| 프레임 폭 상한 | ~~31cm~~ → **기준을 픽셀로 바꿨다** (Q56. 아래 뒤집는 조건 표) |
| 와셔가 찍히는 크기 | **64픽셀** (웹캠·1:1 크롭·`imgsz=640` 실측). 예측값 62.7px과 일치 |
| 아이폰 s01 프레임 폭 | **약 23.2cm** (와셔 역산 22.7 × 보정계수 1.023). 사진별 **21.9~24.9cm** |
| 기기 | 아이폰 15 Pro 광각 → 웹캠 **레이저 Kiyo X** (**별도 세션**) |
| 포맷 | **JPEG** (아이폰 `높은 호환성` 설정. 웹캠이 HEIC를 못 찍어 통일) |
| 아이폰 설정 | **1x (35mm 환산 24mm), 정사각 1:1, 4284 × 4284** — EXIF로 확인(8/19). 아이폰 세션 내내 고정 |
| 트레이 / 배경 | 29cm × 20cm, 갈색 나무 |
| 조명 | **세션마다 바꾼다** — s01·s02 `ceiling` / s03 `ceiling_stand_back` / s04 `stand_back` / s05 `ceiling_stand_side` (D-007 2절). 자연광은 커튼으로 차단 |

**수량** (D-008)

| 항목 | 값 |
|---|---|
| 정상 수량 | 부품당 **3개** |
| 촬영 범위 | 0~6개, 각 개수마다 **5장** |
| 빈 트레이(배경 이미지) | 세션당 1장 = **2.9%** (D-005의 10% 이하 충족. 별도 추가 촬영 불필요) |

**세션과 파일명** (D-009)

| 항목 | 값 |
|---|---|
| 세션 정의 | 한 번 자리를 잡고 앉아 **중단 없이** 이어 찍은 묶음 |
| 한 세션 | **35장** (개수 7종 × 5장) |
| 총 세션 | **6세션** — 아이폰 2(s01 포함) + 웹캠 4 (D-011, 9/8. 기존 10세션 계획을 대체) |
| 분할 배분 | Train 4세션 140장 / Val 1세션 35장 / Test 1세션 35장 (D-011 4절) |
| 세션 ID | `s01`, `s02` … (조건은 넣지 않고 CSV에서 본다) |
| 파일명 | `s01_001.jpg` / 라벨 `s01_001.txt` — 소문자·숫자·`_`만 |
| 폴더 | `dataset/{origin, rename, crop, result_data}` (`crop`은 9/10 추가, 웹캠 전용). `result_data` 아래는 `images/{train,val,test}` + `labels/{train,val,test}` — **`images`·`labels` 이름은 바꿀 수 없다** |
| 메타데이터 | `metadata/sessions.csv`(세션당 1행) + `metadata/images.csv`(사진당 1행). 설명은 `metadata/README.md` |
| 이름 변경 | `scripts/rename_session.py` — **EXIF 촬영 시각** 순으로 `origin/` → `rename/` 복사. 기본은 미리보기, `--apply`로 실행 |
| s01 실제 값 | 높이 22cm(트레이 윗면 기준), 프레임 폭 **23.2cm**(사진별 21.9~24.9), `mount=hand`, 2026-08-18 23:21~23:39 |

**뒤집는 조건 숫자**

| 조건 | 값 |
|---|---|
| 너트↔와셔 상호 오분류율 (Val) | **5% 이상**이면 3종 구성 재검토 (D-004) |
| 아무것도 탐지하지 못하는 비율 | 테스트 이미지의 **10% 이상**이면 배경 이미지 비중 축소 (D-005) |
| ~~프레임 폭~~ → **와셔 픽셀** | **`imgsz=640`에서 40px 미만**이면 촬영 환경 재설계 (D-007 Q56에서 변경. **기기·크롭이 바뀌어도 그대로 쓸 수 있는 기준이라서**) |

---

## 7. 아직 없는 문서

| 문서 | 작성 | 언제 필요한가 |
|---|---|---|
| `docs/experiment-log.md` | **사용자** | 첫 학습(#17) 시작할 때 |
| `docs/questions.md` | **사용자** | 언제든 |
| `README.md` | Claude | 프로젝트 마무리 단계 |
